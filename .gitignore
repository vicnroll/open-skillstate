# Ephemeral worker state under orchestration (ADR 0008)
.openskillstate/worker-state.json

# Integrity record kept outside the state file (ADR 0009)
.openskillstate/.integrity

.DS_Store

# Configuración local de herramientas ajenas al proyecto
.farshid/
